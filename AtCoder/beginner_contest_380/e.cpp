#include <bits/stdc++.h>

using namespace std;

const int N = 5e5 + 10;

int cnt[N], a[N];

int main(){
    int n, q;
    cin >> n >> q;

    for(int i = 1; i <= n; i ++){
        cnt[i] = 1;
        a[i] = i;
    }

    while(q --){
        int t;
        cin >> t;
        if(t == 1){
            int x, c;
            cin >> x >> c;

            int s = 
        }
    }
    
}